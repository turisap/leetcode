const firstUnique = require("./first-unique");
describe("", () => {
  it("should return ", () => {
    expect(firstUnique("leetcode")).toBe(0);
  });

  it("should return ", () => {
    expect(firstUnique("loveleetcode")).toBe(2);
  });
});
