const arrayToBST = (arr) => {
  if (!arr.length) return null;

  const inner = (arr, subtree) => {
    if (!arr.length) return subtree;

    const p = Math.floor(arr.length / 2);
  };
};

module.exports = arrayToBST;
